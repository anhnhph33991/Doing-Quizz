import React from "react";

class MyComponents extends React.Component {
    render() {
        return (
            <div>
                <button>Save new file</button>
                <div>{Math.random()}</div>
            </div>
        )
    }
}

export default MyComponents;